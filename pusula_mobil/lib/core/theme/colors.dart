import 'package:flutter/material.dart';

// ═══════════════════════════════════════════════════════════════════════════════════
// 🎨 PREMIUM DARK THEME - ZAFER RENK PALETİ
// ═══════════════════════════════════════════════════════════════════════════════════

class PColors {
  const PColors._();
  
  // Zafer Renk Paleti - pusulafnl.txt temeli
  static const background = Color(0xFF0D0F14);
  static const surface = Color(0xFF151922);
  static const surfaceHi = Color(0xFF1C2230);
  static const primary = Color(0xFF00C48C);
  static const accent = Color(0xFFFFAA00);
  static const text = Color(0xFFF4F7FB);
  static const textDim = Color(0xFFAAB6C6);
  static const border = Color(0xFF2B3240);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);
  
  // Gradient Colors
  static const gradientStart = Color(0xFF00C48C);
  static const gradientEnd = Color(0xFF0284C7);
  static const gradientPurple = Color(0xFF8B5CF6);
  static const gradientOrange = Color(0xFFFF8C00);
  
  // Level Colors
  static const levelBuyukUsta = Color(0xFFE11D48); // Red for Grand Master
  
  // Impact Area Colors
  static const impactCulture = Color(0xFFAB47BC);   // Purple for culture
  static const impactTechnology = Color(0xFF26C6DA); // Cyan for technology
  
  // Urgency Colors
  static const urgencyMedium = Color(0xFFFF9800);   // Orange for medium urgency
  
  // Code Editor Colors
  static const codeBackground = Color(0xFF23241F);  // Dark gray for code blocks
}
